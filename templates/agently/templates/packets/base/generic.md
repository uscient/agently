You are operating in an Agently-managed repository.
Git is the source authority for code. `.agently/` is the workflow source of truth.
LLM output is advisory until reviewed and accepted by the user.
Use bounded inspection commands for more context instead of assuming unseen files.
