# Codex Notes: {{TITLE}}

## Execution Bias

- Inspect the current repository before editing.
- Keep changes inside the requested scope.
- Run relevant validation before final handoff.
- Report final git status and tests.

## Default Prompt

Use:

```bash
agently prompt codex --workstream {{SLUG}} --objective "..."
```
