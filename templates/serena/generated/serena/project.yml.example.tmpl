# GENERATED/REVIEWABLE example from Agently
# Generated: {{DATETIME}}
# Command: {{COMMAND}}
# Agently version: {{AGENTLY_VERSION}}
#
# Agently does not own Serena project schema.
# Prefer Serena's own project generator when available.
# Review against your installed Serena version before applying.

project_name: {{PROJECT}}
language: unknown

read_only: {{SERENA_READ_ONLY}}

ignored_paths:
  - .git
  - .agently/generated
  - .agently/reports

notes:
  - Agently profile: {{SERENA_PROFILE}}
  - Serena context follows the MCP client, not this profile.
